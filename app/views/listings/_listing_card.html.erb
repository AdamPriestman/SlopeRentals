<div class="card-category">
  <%= cl_image_tag listing.photos.first.key %>
  <div class="card-infos">
    <div>
      <h4><strong><%= listing.name %></strong></h4>
      <p>£<%= listing.price_per_day %> per day </p>
    </div>

      <p><%= listing.user.first_name %> <%= listing.user.last_name %></p>
      <% if listing.user.photo.key? %>
            <%= cl_image_tag listing.user.photo.key%>
        <% else%>
            <p><img src=<%= image_path("unknown.png") %> class="card-seller" alt="User picture" width="50" height="50"></p>
        <% end %>

  </div>
</div>
